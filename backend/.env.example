# Copia este archivo a .env y ajusta la URI de MongoDB
# Ejemplo local:
MONGODB_URI=mongodb://localhost:27017/tacoteca_db

# Ejemplo Atlas (reemplaza <user>, <pass> y <cluster> con tus datos):
# MONGODB_URI=mongodb+srv://<user>:<pass>@<cluster>.mongodb.net/tacoteca_db?retryWrites=true&w=majority
